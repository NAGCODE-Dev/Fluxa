import 'package:flutter/material.dart';

class AppElevation {
  static const cardShadow = [
    BoxShadow(
      color: Color(0x140F172A),
      blurRadius: 40,
      offset: Offset(0, 16),
    ),
  ];
}
