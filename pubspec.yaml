name: financas
description: Aplicativo mobile-first de gestao financeira pessoal com foco em velocidade e privacidade.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  drift: ^2.34.0
  drift_flutter: ^0.3.0
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  sqlite3_flutter_libs: ^0.6.0+eol
  supabase_flutter: ^2.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  drift_dev: ^2.34.1+1
  build_runner: ^2.15.0

flutter:
  uses-material-design: true
